import 'package:flutter/material.dart';

class RoundDetailPage extends StatelessWidget {
  const RoundDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}